#!/usr/bin/env ruby
# frozen_string_literal: true

lib = File.expand_path('../lib/', __dir__)
$LOAD_PATH.unshift lib unless $LOAD_PATH.include?(lib)

require 'pico2wave'
puts "Pico2Wave.version=#{Pico2Wave.version}"

require 'irb'
IRB.start(__FILE__)
